fastapi==0.111.0
uvicorn==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.2
pydantic-settings==2.2.1
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend detection crashes with bcrypt>=4.1
# ("password cannot be longer than 72 bytes"). Pin to the last compatible bcrypt.
bcrypt==4.0.1
httpx==0.27.0
python-multipart==0.0.9
structlog==24.2.0
# google-genai 0.1.1 does not exist on PyPI (versions jump 0.1.0 -> 0.2.0 -> ... -> 1.x).
# Pinning it broke `pip install -r requirements.txt` entirely. Use a valid modern range.
google-genai>=1.0.0,<2.0.0
razorpay>=1.4.2
email-validator>=2.0.0


