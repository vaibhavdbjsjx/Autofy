# Production build config (used by `vite build` for BOTH the Netlify website
# and the Capacitor native app). This is NOT a secret — it's your public
# backend URL — so it is safe to commit.
#
# The native app has no dev proxy, so it needs the absolute backend URL here.
VITE_API_URL=https://autofy-backend-zafr.onrender.com
